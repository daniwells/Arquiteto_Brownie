const size = {
    lt: '320px',
    xs: '385px',
    sm: '480px',
    bs: '630px',
    md: '768px',
    lg: '1024px',
    bg: '1200px',
}

const breakpoints = {
    lt: `(min-width: ${size.lt})`,
    xs: `(min-width: ${size.xs})`,
    sm: `(min-width: ${size.sm})`,
    bs: `(min-width: ${size.bs})`,
    md: `(min-width: ${size.md})`,
    lg: `(min-width: ${size.lg})`,
    bg: `(min-width: ${size.bg})`,
}

export default breakpoints